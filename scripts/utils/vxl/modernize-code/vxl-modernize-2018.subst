#!/bin/sh
case $# in
    0) echo 'Usage: subst files' 1>&2; exit 2
esac

case `uname` in Darwin*) mysed=gsed ;;
  *) mysed=sed ;; esac

for i in $*
do
echo doing $i
ow $i $mysed -f /Users/rfabbri/src/utils-macambira/vxl/modernize-code/vxl-modernize-2018.sed $i
done
