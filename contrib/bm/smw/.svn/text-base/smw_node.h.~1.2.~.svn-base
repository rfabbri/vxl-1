#ifndef SMW_NODE_H
#define SMW_NODE_H

#include<vcl_cmath.h>
#include<vcl_iostream.h>
#include<vcl_vector.h>

class smw_node
{
public:
    //smw_node(float const& mean = 0.0f , float const& sigma = 10.0f,
    //         float const& w = 0.0f)
    //        {mean_ = mean; sigma_ = sigma; n_ = 0; w_ = w;}

    smw_node( float const& mean = 0.0f, float const& sigma = 10.0f,
              float const& w = 0.0f):mean_(mean),sigma_(sigma),w_(w){}

    ~smw_node(){};

    float mean(){return mean_;}

    float sigma(){return sigma_;}

    bool update(float const& observation);

private:
   
    float mean_;
    float sigma_;
    float w_;
    unsigned n_;
};

#endif //SMW_NODE_H
