# contrib/edubrito/CMakeLists.txt
# Developement for : edubrito

INCLUDE_DIRECTORIES( ${lems_SOURCE_DIR}/algo )
INCLUDE_DIRECTORIES( ${lems_SOURCE_DIR}/breye1 )
INCLUDE_DIRECTORIES( ${lems_SOURCE_DIR}/basic )

# camera calibration
SUBDIRS( calibration )
